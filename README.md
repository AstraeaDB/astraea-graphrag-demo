# GraphRAG Demo: A Tale of Two Cities

A demonstration of how a modest 4B-parameter LLM can answer complex literary questions with minimal hallucination by using structured knowledge graph access. The system combines **AstraeaDB** (a vector-property graph database), **Ollama** (local LLM inference with gemma3:4b), and **MCP** (Model Context Protocol) to show that graph structure compensates for model size.

## How It Works

The demo builds a knowledge graph from Charles Dickens's *A Tale of Two Cities* containing 229 nodes (characters, locations, events, themes, chapters, and text passages) connected by 317 edges (relationships, locations, event participation, thematic associations, and provenance links to source text). Every node carries a 128-dimensional embedding for semantic search.

When you ask a question, the system:

1. Embeds your question using EmbeddingGemma (Matryoshka truncation to 128 dims)
2. Sends the question to gemma3:4b with tool descriptions in the system prompt
3. The model outputs structured tool calls (`TOOL_CALL: {"tool": "...", "args": {...}}`), which the orchestrator parses and executes against AstraeaDB's MCP server via JSON-RPC over stdio
4. Tool results (enriched with node names and edge types) are fed back to the model, which can make further tool calls (up to 5 rounds)
5. The model generates a final answer grounded in the graph data

An optional comparison mode shows the same question answered by the raw LLM (no graph) alongside the GraphRAG-augmented answer, making the improvement visible.

**Note on tool calling:** gemma3:4b does not support Ollama's native tool-use API, so the orchestrator implements tool calling through prompt engineering. The model is instructed to output tool calls in a structured JSON format, which the orchestrator parses, executes via MCP, and feeds back.

## Architecture

```
User Question
     │
     ▼
┌──────────────────┐
│   main.py (CLI)  │
│   Interactive     │
│   REPL            │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐         ┌─────────────────┐
│  Orchestrator    │────────►│  Ollama          │
│  (src/)          │◄────────│  gemma3:4b       │
│                  │ prompt-  │  embeddinggemma   │
│                  │ based    └─────────────────┘
│                  │ tool
│                  │ calls
│  MCP Bridge      │         ┌─────────────────┐
│  (JSON-RPC 2.0   │────────►│  AstraeaDB MCP  │
│   over stdio)    │◄────────│  Server (stdio) │
└──────────────────┘         └────────┬────────┘
                              ┌───────┴────────┐
                              │  Knowledge     │
                              │  Graph         │
                              │  229 nodes     │
                              │  317 edges     │
                              │  128-dim HNSW  │
                              └────────────────┘
```

## Knowledge Graph

### Nodes

| Label | Count | Description |
|-------|-------|-------------|
| Character | 29 | People in the novel (Sydney Carton, Lucie Manette, Madame Defarge, etc.) |
| Location | 28 | Places (Tellson's Bank, the Bastille, Defarge wine-shop, etc.) |
| Event | 44 | Key plot events across all three books |
| Theme | 6 | Resurrection, Sacrifice, Duality, Revolution, Fate/Justice, Imprisonment |
| Chapter | 45 | All chapters with summaries |
| TextChunk | 77 | Important passages and quotations from the novel |

### Edges

| Type | Count | Connects |
|------|-------|----------|
| PARTICIPATES_IN | 94 | Character to Event (with role) |
| MENTIONED_IN | 44 | Entity to TextChunk (provenance) |
| NEXT_CHAPTER | 42 | Chapter sequence |
| RELATED_TO | 39 | Character to Character (family, social) |
| OCCURS_DURING | 33 | Event to Chapter |
| OCCURS_IN | 30 | Event to Location |
| LOCATED_IN | 23 | Character/Location to Location |
| EMBODIES | 12 | Character to Theme |

### Embeddings

All 229 nodes have 128-dimensional embeddings generated by EmbeddingGemma via Ollama. The model produces 768-dimensional vectors; we use Matryoshka representation learning to truncate to 128 dimensions and L2 re-normalize. This matches AstraeaDB's HNSW vector index configuration and enables semantic search alongside graph traversal.

## Prerequisites

- **Ollama** with two models:
  ```bash
  ollama pull gemma3:4b
  ollama pull embeddinggemma
  ```
- **AstraeaDB** built from source at `/Users/jamesharris/Documents/astraeadb`
  ```bash
  cd /Users/jamesharris/Documents/astraeadb
  cargo build -p astraea-cli    # debug build includes the mcp subcommand
  ```
- **Python 3.9+** with requests:
  ```bash
  pip install -r requirements.txt
  ```

## Setup

### 1. Generate embeddings (already done -- skip if data/embeddings.json exists)

```bash
python3 scripts/generate_embeddings.py
```

This calls Ollama's embedding API for all 229 entities, truncates to 128 dimensions, and saves to `data/embeddings.json`.

### 2. Start AstraeaDB and load the graph

```bash
# Start the server
/Users/jamesharris/Documents/astraeadb/target/debug/astraea-cli serve &

# Load the knowledge graph (must be repeated after each server restart --
# AstraeaDB stores data in memory by default)
python3 scripts/load_graph.py
```

Output:
```
Connecting to AstraeaDB at 127.0.0.1:7687...
Connected.

Creating nodes...
  Characters: 29
  Locations: 28
  Events: 44
  Themes: 6
  Chapters: 45
  TextChunks: 77

Total nodes created: 229

Creating edges...
  Edges created: 317

Graph loaded successfully!
```

### 3. Run the tests

```bash
python3 test_demo.py
```

This verifies all four layers: data files, Ollama models, MCP bridge tools, and the orchestrator. All 24 tests should pass.

### 4. Run the demo

```bash
# Standard mode
python3 main.py

# With comparison mode (raw LLM vs GraphRAG side-by-side)
python3 main.py --compare

# With verbose output (shows MCP tool calls)
python3 main.py --verbose

# Both
python3 main.py --compare --verbose
```

## Usage

The demo presents an interactive prompt. Type questions about the novel or use commands:

| Command | Description |
|---------|-------------|
| *(any question)* | Query the knowledge graph and get an answer |
| `/compare` | Toggle comparison mode on/off |
| `/verbose` | Toggle verbose mode (shows tool calls) |
| `/stats` | Display knowledge graph statistics |
| `/tools` | List available MCP tools |
| `/quit` | Exit |

### Example session

```
Question: How are Charles Darnay and the Marquis St. Evremonde related?

  Querying gemma3:4b with GraphRAG via MCP...

[With GraphRAG]
Charles Darnay is the nephew of the Marquis St. Evremonde. The graph shows
a RELATED_TO edge with relationship type "nephew" between them. Darnay
renounced his family name and inheritance due to his uncle's cruelty toward
the French peasantry, adopting the name "Darnay" to distance himself from
the Evremonde legacy.
```

With `--verbose`, you also see the tool calls:

```
  [Tool] find_by_label({"label": "Character"})
  [Tool] get_node({"id": 1772})
  [Tool] neighbors({"id": 1772, "edge_type": "RELATED_TO"})
```

### Comparison mode

```
Question: Who is Madame Defarge and what drives her actions?

[Without Graph]
(gemma3:4b's best guess -- may hallucinate details, e.g. inventing
a backstory or confusing character relationships)

[With GraphRAG]
(Grounded answer citing specific characters, events, and relationships
from the knowledge graph)
```

## Available MCP Tools

The LLM has access to 10 tools from AstraeaDB's MCP server. The MCP bridge enriches results from `find_by_label`, `neighbors`, and `vector_search` with human-readable node names, labels, and edge types so the model can navigate the graph effectively.

| Tool | Purpose |
|------|---------|
| `find_by_label` | Find all nodes of a type (Character, Location, Event, etc.) -- enriched with names |
| `get_node` | Get full details of a node by ID |
| `neighbors` | Explore a node's relationships -- enriched with edge types and target names |
| `shortest_path` | Find the connection path between two nodes |
| `vector_search` | Semantic similarity search using embeddings -- enriched with names and descriptions |
| `hybrid_search` | Combined vector + graph proximity search |
| `extract_subgraph` | Get a readable text summary of a node's neighborhood |
| `bfs` | Breadth-first traversal from a node |
| `query` | Execute GQL (Graph Query Language) queries |
| `graph_stats` | Knowledge graph statistics |

## Configuration

All settings can be overridden via environment variables:

| Variable | Default | Description |
|----------|---------|-------------|
| `ASTRAEA_HOST` | `127.0.0.1` | AstraeaDB server host |
| `ASTRAEA_PORT` | `7687` | AstraeaDB server port |
| `ASTRAEA_BIN` | `target/debug/astraea-cli` | Path to astraea-cli binary (must have `mcp` subcommand -- debug build) |
| `OLLAMA_URL` | `http://localhost:11434` | Ollama API endpoint |
| `CHAT_MODEL` | `gemma3:4b` | Ollama chat model |
| `EMBED_MODEL` | `embeddinggemma` | Ollama embedding model |

## Project Structure

```
graphrag-demo/
├── main.py                    # CLI entry point (REPL with /compare, /stats, /tools, /verbose)
├── test_demo.py               # Test suite (24 tests across 4 layers)
├── requirements.txt           # Python dependencies (requests)
├── data/
│   ├── characters.json        # 29 character entities
│   ├── locations.json         # 28 location entities
│   ├── events.json            # 44 plot events
│   ├── themes.json            # 6 themes
│   ├── chapters.json          # 45 chapter summaries
│   ├── text_chunks.json       # 77 key passages
│   ├── edges.json             # 317 relationships
│   └── embeddings.json        # 128-dim vectors for all nodes
├── scripts/
│   ├── generate_embeddings.py # Embed entities via Ollama + Matryoshka truncation
│   ├── load_graph.py          # Load data into AstraeaDB via Python client
│   └── merge_extractions.py   # Merge per-book extractions into unified data/
├── src/
│   ├── config.py              # Configuration (paths, model names, defaults)
│   ├── embeddings.py          # EmbeddingGemma via Ollama (128-dim Matryoshka)
│   ├── mcp_bridge.py          # MCP JSON-RPC bridge to AstraeaDB + result enrichment
│   └── orchestrator.py        # Prompt-based tool-calling loop: Ollama <-> MCP
└── knowledge_graph/
    ├── book1_entities.json    # Per-book extraction (intermediate)
    ├── book2_entities.json
    └── book3_entities.json
```

## How the Knowledge Graph Was Built

1. The full text of *A Tale of Two Cities* (Project Gutenberg) was processed book-by-book
2. Claude extracted characters, locations, events, chapter summaries, and key text passages with structured JSON output
3. Coreference resolution merged duplicate entities (e.g., "Dr. Manette" / "Alexandre Manette" / "One Hundred and Five, North Tower")
4. Themes and thematic edges (EMBODIES) were added based on literary analysis
5. Per-book extractions were merged via `scripts/merge_extractions.py`, normalizing IDs and deduplicating
6. All entity descriptions and text passages were embedded using EmbeddingGemma (768-dim, truncated to 128 via Matryoshka)
7. Everything was loaded into AstraeaDB with embeddings attached to nodes for HNSW indexing
